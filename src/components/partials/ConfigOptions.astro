---
// @ts-nocheck
import ConfigOption from '../ConfigOption.astro'
import {type ConfigItem, fetchConfigItems} from '../../helpers/config'

const data: ConfigItem = await fetchConfigItems()
---

{data.children?.map(c => (<ConfigOption item={c}/><hr/>))}
