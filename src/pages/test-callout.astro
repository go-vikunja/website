---
import Layout from '../layouts/Layout.astro'
import Prose from '../components/Prose.astro'
import Callout from '../components/Callout.astro'
---

<Layout title="Test Callout">
    <main class="mx-auto max-w-(--breakpoint-lg) px-4 md:px-0 py-8">
        <Prose>
            <h1>Callout Test Page</h1>
            
            <p>This page tests different callout types to verify bold text behavior in dark mode.</p>
            
            <Callout type="warning">
                <p>This is a <strong>warning callout</strong> with some <strong>bold text</strong> that should be black in dark mode.</p>
            </Callout>
            
            <Callout type="info">
                <p>This is an <strong>info callout</strong> with some <strong>bold text</strong>.</p>
            </Callout>
            
            <p>Regular text with <strong>bold text</strong> outside of callouts.</p>
        </Prose>
    </main>
</Layout>